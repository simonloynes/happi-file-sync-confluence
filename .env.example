# Happi File Sync Confluence - Local Development Environment
# Copy this file to .env and fill in your values

# Confluence Configuration (Option 1: Multiline JSON)
INPUT_FILE_MAPPINGS='{
  "baseUrl": "https://your-company.atlassian.net/wiki",
  "user": "your-email@company.com",
  "pass": "your-api-token-or-password",
  "personalAccessToken": "",
  "force": false,
  "fileRoot": "./test-data",
  "pages": [
    {
      "pageId": "123456789",
      "file": "sample.md",
      "title": "Test Page from Local Development",
      "spaceKey": "DEV",
      "parentId": ""
    }
  ]
}'

# Alternative Option 2: Single-line JSON (escape quotes and remove line breaks)
# INPUT_FILE_MAPPINGS='{"baseUrl":"https://your-company.atlassian.net/wiki","user":"your-email@company.com","pass":"your-api-token","personalAccessToken":"","force":false,"fileRoot":"./test-data","pages":[{"pageId":"123456789","file":"sample.md","title":"Test Page from Local Development","spaceKey":"DEV","parentId":""}]}'

# Alternative Option 3: Use config files instead (recommended for complex setups)
# Just copy and modify one of the files in test-data/configs/ and use:
# tsx src/local-runner.ts --config path/to/your-config.json --dry-run

# Debug logging (true/false)
INPUT_DEBUG=true

# Alternative: Use separate environment variables instead of JSON
# (Comment out INPUT_FILE_MAPPINGS above and use these instead)

# CONFLUENCE_BASE_URL=https://your-company.atlassian.net/wiki
# CONFLUENCE_USER=your-email@company.com  
# CONFLUENCE_PASSWORD=your-api-token-or-password
# CONFLUENCE_PAT=your-personal-access-token
# CONFLUENCE_SPACE_KEY=DEV
# CONFLUENCE_PAGE_ID=123456789
# CONFLUENCE_FILE=test-data/sample.md
# CONFLUENCE_TITLE=Test Page from Local Development

# Security Notes:
# - Never commit this file with real credentials
# - Use API tokens instead of passwords when possible
# - Consider using Personal Access Tokens for better security
# - The .env file is already in .gitignore